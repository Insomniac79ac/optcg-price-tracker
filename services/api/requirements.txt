fastapi
uvicorn[standard]
python-multipart
pydantic-settings
pytest
httpx
sqlalchemy>=2.0
alembic
psycopg[binary]
redis
celery
pyjwt
argon2-cffi
# Inspection only - decoding, dimensions, format and alpha for the
# display-image mirroring verifier (app.services.display_image_mirror). It
# must never produce image bytes: no Image.save, re-encode, resize or crop.
Pillow
# Perceptual hashing for app.services.artwork_evidence. Pinned to the same
# version the snkrdunk collector uses: the two compute distances that are
# compared against shared thresholds, so a version skew between them would
# silently move the numbers. artwork_evidence imports it lazily, which is why
# its absence here surfaced only as an "unusable" verdict at runtime rather
# than an import error - see tests/test_artwork_evidence.py.
ImageHash==4.3.2
# Listed although ImageHash already pulls it in, unlike botocore below: the
# connected-component sweep in app.services.artwork_evidence._foreground_regions
# imports numpy DIRECTLY, and that import is lazy too. An undeclared direct
# import fails exactly the way the ImageHash one did - every verdict comes back
# "unusable" and nothing else looks wrong. Unpinned deliberately: ImageHash
# pins no numpy version, and pinning one here could contradict it.
numpy
# Cloudflare R2 via its S3-compatible API (app.services.object_storage) - the
# only S3 client in this service. botocore is pulled in transitively and is
# deliberately not listed separately, matching the convention above. R2 is
# never contacted unless an R2ObjectStorage is explicitly constructed, so the
# API starts and every unrelated test runs with all R2_* settings unset.
boto3
