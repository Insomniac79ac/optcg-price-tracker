# Skeleton only — no services are runnable yet. Placeholders for future implementation.

version: "3.9"

services:
  postgres:
    # image: postgres:16
    # ports:
    #   - "5432:5432"
    # environment:
    #   POSTGRES_USER: opcg
    #   POSTGRES_PASSWORD: opcg
    #   POSTGRES_DB: opcg
    # volumes:
    #   - postgres_data:/var/lib/postgresql/data

  redis:
    # image: redis:7
    # ports:
    #   - "6379:6379"

  api:
    # build: ./services/api
    # ports:
    #   - "8000:8000"
    # env_file:
    #   - .env
    # depends_on:
    #   - postgres
    #   - redis

  worker:
    # build: ./services/worker
    # env_file:
    #   - .env
    # depends_on:
    #   - postgres
    #   - redis

  web:
    # build: ./apps/web
    # ports:
    #   - "3000:3000"
    # env_file:
    #   - .env
    # depends_on:
    #   - api

# volumes:
#   postgres_data:
