# Release blockers - v1.0.0

**No open release blockers.** Every row below is `fixed` or `deferred` (cosmetic, non-blocking) as
of the 2026-07-23 v1.0.0 tagging pass - see `docs/release_candidate_report.md` for the full
verification record (tests, audits, secrets check all re-run clean against this commit).

Tracked list of issues found during the Phase 11 release-candidate audit (see
`scripts/release_candidate_audit.sh` and `docs/release_candidate_report.md`) that could affect the
`v1.0.0` tag decision. Add a row whenever the audit (or manual QA) turns up something worth
tracking - not every `WARN` from the audit script needs a row here, only the ones a human decides
are actually worth tracking to resolution.

| ID | Severity | Area | Issue | Status | Owner | Notes |
|----|----------|------|-------|--------|-------|-------|
| RC-1 | blocker | Backend tests | `docker compose exec api pytest` aborted collection entirely (0 tests ran) - `tests/test_phase9_audit_script.py` and `tests/test_release_candidate_audit_script.py` computed the repo root as `Path(__file__).resolve().parents[3]`, which raises `IndexError` inside the api Docker image (only `services/api` is copied to `/app`, so the parent chain is shallower than a dev checkout). | fixed | - | Added `services/api/tests/_repo_root.py` (mirrors `services/worker/worker/path_utils.py`'s `find_project_root`) and skip these two files with `pytest.mark.skipif` when the repo root isn't visible, instead of crashing at import time. Regression test added: `services/api/tests/test_repo_root.py` (4 cases, using scaffolded `tmp_path` dirs so it's deterministic in both a full checkout and the container's shallow layout - confirmed the fixed test files correctly `skip` rather than crash when run via `docker compose exec api pytest`). |
| RC-2 | blocker | Backend tests | `test_portfolio_valuation_snapshots_thinning_keeps_one_per_week_when_old` is flaky: it derives two "old" timestamps as `NOW - 200d` and `NOW - 199d` and asserts they land in the same ISO week, which is false whenever `NOW - 200d` falls on a Sunday (as it did on the day this audit ran - 2026-01-04). Not an implementation bug in the thinning logic itself. | fixed | - | Anchored the test's `base` timestamp to the Monday of its ISO week before adding the second snapshot, so the assertion no longer depends on what day the suite happens to run. |
| RC-3 | high | Release tooling / secrets check | `scripts/check_secrets.sh` section 4 false-positived on every run: it split each matched line on the line's *first* `=` rather than the matched secret key's own `=`, so lines like `SKIP_TESTS=true ... ADMIN_TOKEN="$ADMIN_TOKEN"` were judged by `SKIP_TESTS`'s value. After narrowing the extraction to the actual matched token, a second false positive appeared: the match regex excluded `$` entirely, so it truncated `"${ADMIN_TOKEN:-local-dev-admin-token}"` before the `$`, still misreading legitimate shell-variable references as literal secrets. | fixed | - | Rewrote the section-4 extraction to use `git grep -oE` on the actual `KEY=value` token (not the whole line) and widened the value-match regex so `$` can appear after the first character. `check_secrets.sh` now passes cleanly; no real secret was ever present - this was 100% a static-check bug (see Part 5 rule: narrow the check, don't remove it). |
| RC-4 | high | Release tooling / phase10 audit | `scripts/phase10_ux_audit.sh`'s `PUBLIC_GET_CHECKS` demanded exactly `200` from `/saved-views`, `/analytics/digest`, `/analytics/buy-decisions`, `/analytics/sell-decisions` - all four have always required `require_current_user` (session auth) since the commit that introduced them, so an unauthenticated request correctly gets `401`. This is the same session-gating `scripts/release_candidate_audit.sh` already treats as a pass. | fixed | - | Split these four into a `SESSION_GATED_GET_CHECKS` list that accepts `200` or `401`, matching `release_candidate_audit.sh`'s existing convention. Auth was not weakened or removed. |
| RC-5 | high | Admin safety | `/admin/cards`' card-catalog CSV import ("Import for real", with an optional "Overwrite existing fields" checkbox) ran on a single click with no typed confirmation - inconsistent with every other real/destructive admin action in the app (backup restore-replace requires typing `RESTORE`, data-retention prune requires typing `PRUNE`, cache clear requires typing `CLEAR`, job-lock force-release requires typing `RELEASE`). `ConfirmActionModal`'s own docstring already lists `"IMPORT"` as an anticipated `confirmPhrase` example, suggesting this page was simply missed. | fixed | - | Wired `ConfirmActionModal` (typed `IMPORT` phrase) in front of the real-import call in `apps/web/src/app/admin/cards/page.tsx`, matching the pattern used on `/admin/card-duplicates` and `/admin/source-mapping-quality`. Regression test added in `page.test.tsx` (`requires typing IMPORT to confirm...`). |
| RC-6 | high | Frontend test infra | Every single React component test in `apps/web` (all `*.test.tsx` under `src/app` and `src/components`) fails with `TypeError: React.act is not a function` when run the same way the frontend build is run (`docker compose exec web ...`). Root cause: `apps/web/Dockerfile` sets `ENV NODE_ENV=production` (correct for `next start`), and that baked env poisons `vitest` too - React 19's production build strips `act()` entirely. Not caused by any Phase 1-10 code; purely a container/script wiring gap. Not part of Part 1's required command list, but caught while adding a Part 8 regression test and worth fixing since it silently made the entire component-test suite unrunnable via the documented container workflow. | fixed | - | Changed `apps/web/package.json`'s `"test"` script to `"NODE_ENV=test vitest run"` so it always runs under the right mode regardless of the ambient container env. Verified: `docker compose exec web npm test` now passes across the suite (see release_candidate_report.md's test checklist). |
| RC-7 | low | Frontend build | `docker compose exec web npm run build` succeeds (exit 0) but prints a Turbopack warning about `fs`/`path` tracing rooted at `next.config.ts` -> `src/app/api/version/route.ts`. The route already uses the recommended `/* turbopackIgnore: true */` annotation on its `process.cwd()` calls; this is Turbopack being conservative, not a functional bug. | deferred | - | No action taken - cosmetic build-log noise only, build output and route behavior are correct. Revisit only if a future Next.js/Turbopack upgrade turns this into an actual failure. |
| RC-8 | low | Release process | Git working tree had uncommitted changes for the duration of this Phase 11 pass (the fixes for RC-1 through RC-6 above, plus this doc). `scripts/release_candidate_audit.sh` (default) and `scripts/phase10_ux_audit.sh`/`scripts/final_audit.sh` (`ALLOW_DIRTY=false`/`STRICT_GIT=true`) correctly flagged this at the time. | fixed | - | Resolved once those changes were reviewed and committed (see `a341e0b`, `6ecee3f`). `git status` is clean as of the 2026-07-23 v1.0.0 tagging pass; `scripts/final_audit.sh` (default, no `ALLOW_DIRTY`) re-run and passes against the clean tree. |
| RC-9 | high | Release tooling - all `scripts/*.sh` audit/release scripts | Every audit/release script (`check_secrets.sh`, `release_candidate_audit.sh`, `final_audit.sh`, `phase7-10_audit.sh`, `release_check.sh`, `prod_verify.sh`) resolved its repo root via `git rev-parse --show-toplevel`, which depends on the *caller's current working directory*. Invoked from outside the repo entirely (e.g. `bash /path/to/scripts/foo.sh` from `/`), that lookup fails: scripts using `set -e` (`check_secrets.sh`, `final_audit.sh`, `phase7-10_audit.sh`) aborted immediately with a raw `fatal: not a git repository` message; scripts using only `set -uo pipefail` without `-e` (`release_candidate_audit.sh`, `release_check.sh`, `prod_verify.sh`) silently continued with an empty `repo_root`, `cd ""` was a no-op, and every subsequent `docker compose` call then failed with the confusing "no configuration file provided: not found" - the exact symptom reported. Also: `docker-compose.yml`'s `web` service hardcoded `"3000:3000"`, so there was no way to run the dev stack alongside another stack already using port 3000 (e.g. `opcg-web-prod`) without a manual override file. | fixed | - | Every listed script now derives its repo root from its own file location (`SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`; `repo_root="$(cd "$SCRIPT_DIR/.." && pwd)"`), independent of the caller's CWD. `docker-compose.yml`'s `web` port is now `"${WEB_PORT:-3000}:3000"` (container-internal port unchanged; production compose untouched). Verified: `bash /workspaces/optcg-price-tracker/scripts/check_secrets.sh` and `.../release_candidate_audit.sh` both now run correctly from `/`; `WEB_PORT=3001 docker compose up -d --build api web worker` runs dev web on 3001 with the pre-existing `opcg-web-prod` still on 3000, untouched. See `docs/release_candidate_report.md`'s "Running the dev stack when port 3000 is occupied" for the documented workflow. |
| RC-10 | low | Release tooling - `scripts/release_candidate_audit.sh` route detection | Post-v1.0.0 cleanup: `web_route_exists`/`check_web_route` and the `ConfirmActionModal` admin-safety grep hardcoded `apps/web/src/app` as the Next.js app directory, instead of detecting it. This repo only ever had `apps/web/src/app` (never `apps/web/app`), so it never actually caused a false warning - but the two persistent warnings for `/admin/source-mappings` and `/admin/env-check` (neither has a frontend page, confirmed by inspecting `apps/web/src/app/admin`) were worth double-checking weren't a directory-detection artifact. | fixed | - | Script now detects `WEB_APP_DIR` once at startup (`apps/web/src/app` if present, else `apps/web/app`, hard-`FAIL`s if neither exists) and uses that variable in place of the hardcoded path everywhere a route or the admin directory is checked. Re-ran `BASE_WEB_URL=http://127.0.0.1:3001 SKIP_TESTS=true RUN_PHASE_AUDITS=false bash scripts/release_candidate_audit.sh` - same two warnings reproduce, confirming they're a genuine route-inventory gap (no frontend page exists for either route), not a false positive. No product features, formulas, or scraping behavior changed; `v1.0.0` was not retagged. |

## Severity definitions

- **blocker** - must be fixed before tagging `v1.0.0`. Data loss, a broken migration, a secret
  committed to git, or a route that's completely down.
- **high** - should be fixed before tagging, but a same-day follow-up patch release is an
  acceptable fallback if there's a real reason to ship now anyway.
- **medium** - worth fixing soon, but doesn't block the tag. Cosmetic-but-confusing UI issues,
  a non-critical admin page returning a warning status, missing-but-not-required documentation.
- **low** - nice to have. Polish, minor copy issues, optional tooling gaps.

## How to use this table

1. When `scripts/release_candidate_audit.sh` reports a `WARN`, decide whether it's just noise (a
   route the script guessed at that never existed, e.g.) or a real gap worth tracking. Only the
   latter gets a row.
2. Give it an ID (`RC-1`, `RC-2`, ...), a severity from the table above, and the area it affects.
3. Update `Status` as it moves (`open` -> `in progress` -> `fixed` -> `verified`).
4. Before tagging `v1.0.0`, every `blocker` row must be `fixed`/`verified`. `high` rows should be
   `fixed`/`verified` or have an explicit, written reason for shipping anyway in `Notes`. As of the
   2026-07-23 tagging pass, all rows are `fixed` except RC-7 (`deferred`, cosmetic-only).
